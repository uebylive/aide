# CodeStory Authentication for Aide

**Notice:** This extension is bundled with Aide. It can be disabled but not uninstalled.

## Features

This extension provides support for authenticating to CodeStory. It registers the `codestory` Authentication Provider that can be leveraged by other extensions. This also provides the CodeStory authentication used by Settings Sync.
